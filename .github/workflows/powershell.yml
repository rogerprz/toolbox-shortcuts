name: Powershell

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: windows-latest

    steps:
    - name: run powershell
      shell: powershell -NoProfile -Command "& '{0}'"
      run: |-
        echo $Env:Path
        echo "We did it"
        
